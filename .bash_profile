#
# ~/.bash_profile
#

# NOTE: this file doesn't get sourced on start i3
# I think this is because it is non-login shell?

[[ -f ~/.bashrc ]] && . ~/.bashrc
[[ -f ~/.bash_options ]] && source ~/.bash_options
